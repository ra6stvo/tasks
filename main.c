#include <stdio.h>

void the_equation()
{
	int a;
	int b;
	
	scanf("%d%d", &a, &b);

	if (a == 0 && b == 0)
	{
		printf("INF\n");
	}
	else if (a == 0)
	{
		printf("NO\n");
	}
	else
	{
		printf("%d\n", -b / n);
	}
}

int main()
{
	the_equation();
	return 0;
}
